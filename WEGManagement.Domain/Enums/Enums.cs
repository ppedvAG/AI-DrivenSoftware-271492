namespace WEGManagement.Domain.Enums
{
    public enum ApartmentStatus { Rented, Sold, Empty }
    public enum PaymentStatus { Open, Paid, Overdue }
    public enum PaymentType { Transfer, Cash }
    public enum RepairOrderPriority { Low, High }
    public enum RepairOrderStatus { Open, InProgress, Completed }
}
